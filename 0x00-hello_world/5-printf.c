#include <stdio.h>
/**
 * main - prints using printf
 *
 * Return: always 0
 */

int main(void)
{
	char mystr[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", mystr);

	return (0);
}
