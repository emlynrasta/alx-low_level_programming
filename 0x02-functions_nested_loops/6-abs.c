#include <stdio.h>
#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @c : int to be computed
 *
 * Return: always 0
 */

int _abs(int c)
{
	if (n >= 0)
		return (0);
	else
		return (-c);
}
